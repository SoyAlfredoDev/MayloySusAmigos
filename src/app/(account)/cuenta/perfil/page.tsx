import { AccountPageHeader } from "@/components/account";

export const metadata = { title: "Mi Perfil" };

export default function ProfilePage() {
  return (
    <AccountPageHeader
      title="Mi perfil"
      description="Datos del Pet Parent — Fase 1"
    />
  );
}
